language: java
addons:
  sonarcloud:
    organization: "surite-github"
    token:
      secure: $SONAR_TOKEN
jdk:
  - oraclejdk8
jobs:
  include:
    - stage: unit test
      script: gradle build
    - stage: code scan
      if: $TRAVIS_PULL_REQUEST = false
      script: gradle sonarqube
